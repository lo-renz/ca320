Q1. Write an expression that evaluates to a list of the odd number that are a multiple of 7 and less than 100.
Q2. Write a function that takes two integers and evaluates to their sum if either integer is odd, otherwise evaluates to their product.
Q3. Write a function, join, that takes two lists and returns a list that is the concatenation of both lists except that elements in the first list that are already in the second list are not added to the resulting list, e.g. join [1,2,3] [2,4,6,8] evaluates to (1,3,2,4,6,8).
(1 marks)
(2 mark)
(3 marks)
04. Write a data type for a binary tree.
Q5. Write a function that converts a binary tree into a list by doing a preorder traversal of the binary tree. Remember a preorder traversal visits the parent node, then the left child and then the right child.
(1 marks)
(3 marks)